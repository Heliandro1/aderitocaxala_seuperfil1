import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginTop: 15,
  },
  linkText: {
    color: "#07689F",
    fontWeight: '700',
    textAlign: 'center',
  },
});

export default styles;
